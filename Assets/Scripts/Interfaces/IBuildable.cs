using UnityEngine;

public interface IBuildable
{
    //interface for everything that you can put down in the buildable mode

    //it needs an easy way of determining what the grid locations are going to be
}
